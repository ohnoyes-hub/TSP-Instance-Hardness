# This file extract all the iterations, hardness, initial and hardest matrix, 

# Extract the following values into one superfile:
# json format: configuration: {mutation_type, generation_type, distribution, city_size}, run_number: int, number_iterations, iterations: array, hardest: array, initial_matrix: 2d-array, hardest_matrices: 2d-array

import os
import json